<h2><%=t :register %></h2>

<div class="float_left standard_width">

  <fieldset>
    <legend><%=t :enter_your_details %></legend>
<%= error_messages_for 'user' %>
<% form_for(@user) do |f| %>
    <div class="form_row">
      <label for="username"><%=t :username %></label>
    <%= f.text_field :username, :size => User::USERNAME_SIZE, :maxlength => User::USERNAME_MAX_LENGTH%>
    </div>

    <div class="form_row">
    <label for="email"><%=t :email %></label>
    <%= f.text_field :email, :size => User::EMAIL_SIZE, :maxlength => User::EMAIL_MAX_LENGTH %><br />
    <span class="smaller"><%= t(:email_hint) %></span>
    </div>

    <div class="form_row">
    <label for="password"><%=t :password %></label>
    <%= f.password_field :password, :size => User::PASSWORD_SIZE, :maxlength => User::PASSWORD_MAX_LENGTH %>
    </div>

    <div class="form_row">
    <label for="password_confirn"><%=t :password_confirm %></label>
    <%= f.password_field :password_confirmation, :size => User::PASSWORD_SIZE, :maxlength => User::PASSWORD_MAX_LENGTH %>
    </div>

    <div class="form_row">
    <%= f.submit t(:register_button), :class => "submit" %>
    </div>

<% end %>

  </fieldset>

</div>

<div class="float_right standard_width">
    <fieldset>
      <legend><%=t :or_login_with_Facebook %></legend>
        <p><%= t(:facebook_explanation) %></p>
        <%= fb_login_button('window.location = "/users/link_user_accounts";')%>
   </fieldset>
</div>
